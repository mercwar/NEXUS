name: Manual Bash Run
on: workflow_dispatch # This allows you to click a button to run it

jobs:
  execute-commands:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run my commands
        run: |
          echo "Hello from the GitHub runner!"
          ls -la
          node -v
          # Add any other bash commands here
